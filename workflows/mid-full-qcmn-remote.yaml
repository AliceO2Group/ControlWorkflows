name: mid-full-qcmn-remote
vars:
  dpl_command: >-
    o2-qc --config {{ qc_config_uri }} --remote -b
defaults:
  qc_remote_machine: "alio2-cr1-qme02"
  qc_config_uri: "consul-json://{{ consul_endpoint }}/o2/components/qc/ANY/any/mid-full-qcmn"
  monitoring_dpl_url: "no-op://"
  user: "flp"
  fmq_rate_logging: 0
  shm_segment_size: 10000000000
  shm_throw_bad_alloc: false
  session_id: default
  resources_monitoring: 15
constraints:
  - attribute: machine_id
    value: "{{ qc_remote_machine }}"
roles:
  - name: "internal-dpl-clock"
    connect:
    task:
      load: mid-full-qcmn-remote-internal-dpl-clock
  - name: "MID-QcTaskMIDDigits-proxy"
    connect:
    - name: from_internal-dpl-clock_to_MID-QcTaskMIDDigits-proxy
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.internal-dpl-clock:from_internal-dpl-clock_to_MID-QcTaskMIDDigits-proxy"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    bind:
    - name: MID-QcTaskMIDDigits-proxy
      type: sub
      transport: zeromq
      addressing: tcp
      rateLogging: "{{ fmq_rate_logging }}"
      target: "tcp://*:47780"
    task:
      load: mid-full-qcmn-remote-MID-QcTaskMIDDigits-proxy
  - name: "MID-QcTaskMIDRaw-proxy"
    connect:
    - name: from_internal-dpl-clock_to_MID-QcTaskMIDRaw-proxy
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.internal-dpl-clock:from_internal-dpl-clock_to_MID-QcTaskMIDRaw-proxy"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    bind:
    - name: MID-QcTaskMIDRaw-proxy
      type: sub
      transport: zeromq
      addressing: tcp
      rateLogging: "{{ fmq_rate_logging }}"
      global: "MID-QcTaskMIDRaw-proxy"
    task:
      load: mid-full-qcmn-remote-MID-QcTaskMIDRaw-proxy
  - name: "MERGER-QcTaskMIDDigits1l-0"
    connect:
    - name: from_internal-dpl-clock_to_MERGER-QcTaskMIDDigits1l-0
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.internal-dpl-clock:from_internal-dpl-clock_to_MERGER-QcTaskMIDDigits1l-0"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    - name: from_MID-QcTaskMIDDigits-proxy_to_MERGER-QcTaskMIDDigits1l-0
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.MID-QcTaskMIDDigits-proxy:from_MID-QcTaskMIDDigits-proxy_to_MERGER-QcTaskMIDDigits1l-0"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: mid-full-qcmn-remote-MERGER-QcTaskMIDDigits1l-0
  - name: "MERGER-QcTaskMIDRaw1l-0"
    connect:
    - name: from_internal-dpl-clock_to_MERGER-QcTaskMIDRaw1l-0
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.internal-dpl-clock:from_internal-dpl-clock_to_MERGER-QcTaskMIDRaw1l-0"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    - name: from_MID-QcTaskMIDRaw-proxy_to_MERGER-QcTaskMIDRaw1l-0
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.MID-QcTaskMIDRaw-proxy:from_MID-QcTaskMIDRaw-proxy_to_MERGER-QcTaskMIDRaw1l-0"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: mid-full-qcmn-remote-MERGER-QcTaskMIDRaw1l-0
  - name: "qc-check-MID-QcCheckMID"
    connect:
    - name: from_MERGER-QcTaskMIDRaw1l-0_to_qc-check-MID-QcCheckMID
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.MERGER-QcTaskMIDRaw1l-0:from_MERGER-QcTaskMIDRaw1l-0_to_qc-check-MID-QcCheckMID"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: mid-full-qcmn-remote-qc-check-MID-QcCheckMID
  - name: "qc-check-sink-QC_QcTaskMIDDigits_0"
    connect:
    - name: from_MERGER-QcTaskMIDDigits1l-0_to_qc-check-sink-QC_QcTaskMIDDigits_0
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.MERGER-QcTaskMIDDigits1l-0:from_MERGER-QcTaskMIDDigits1l-0_to_qc-check-sink-QC_QcTaskMIDDigits_0"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: mid-full-qcmn-remote-qc-check-sink-QC_QcTaskMIDDigits_0
  - name: "internal-dpl-injected-dummy-sink"
    connect:
    - name: from_qc-check-MID-QcCheckMID_to_internal-dpl-injected-dummy-sink
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.qc-check-MID-QcCheckMID:from_qc-check-MID-QcCheckMID_to_internal-dpl-injected-dummy-sink"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: mid-full-qcmn-remote-internal-dpl-injected-dummy-sink
  - name: fairmq-shmmonitor
    enabled: "{{fmq_cleanup_enabled == 'true'}}"
    task:
      load: "fairmq-shmmonitor"
      trigger: DESTROY
      timeout: 10s
      critical: false
