name: mft-full-qcmn-local
vars:
  dpl_command: >-
    o2-dpl-raw-proxy -b --session default --dataspec 'x:MFT/RAWDATA;dd:FLP/DISTSUBTIMEFRAME/0' --readout-proxy '--channel-config "name=readout-proxy,type=pull,method=connect,address=ipc:///tmp/stf-builder-dpl-pipe-0,transport=shmem,rateLogging=10"' | o2-datasampling-standalone -b --config {{ ds_config_uri }} | o2-itsmft-stf-decoder-workflow -b --runmft --digits --no-clusters --dataspec TF:MFT/RAWDATA_SMP --ignore-dist-stf --configKeyValues 'MFTClustererParam.noiseFilePath=/tmp/_DUMMY_;MFTClustererParam.dictFilePath=/tmp/_DUMMY_' | o2-dpl-output-proxy -b --session default --dataspec 'x:MFT/RAWDATA;dd:FLP/DISTSUBTIMEFRAME/0' --dpl-output-proxy '--channel-config "name=downstream,type=push,method=bind,address=ipc:///tmp/stf-pipe-0,rateLogging=10,transport=shmem"' | o2-qc --config {{ qc_config_uri }} -b --local --host flp
defaults:
  ds_config_uri: "consul-json://{{ consul_endpoint }}/o2/components/qc/ANY/any/mft-full-digits-sampling"
  qc_config_uri: "consul-json://{{ consul_endpoint }}/o2/components/qc/ANY/any/mft-full-qcmn-{{ it }}"
  monitoring_dpl_url: "no-op://"
  user: "flp"
  fmq_rate_logging: 0
  shm_segment_size: 10000000000
  shm_throw_bad_alloc: false
  session_id: default
  resources_monitoring: 15
roles:
  - name: "internal-dpl-clock"
    connect:
    task:
      load: mft-full-qcmn-local-internal-dpl-clock
  - name: "readout-proxy"
    connect:
    - name: from_internal-dpl-clock_to_readout-proxy
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.internal-dpl-clock:from_internal-dpl-clock_to_readout-proxy"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    - name: readout-proxy
      type: pull
      transport: shmem
      target: "::readout-proxy-{{ it }}"
      rateLogging: "{{ fmq_rate_logging }}"
    task:
      load: mft-full-qcmn-local-readout-proxy
  - name: "Dispatcher"
    connect:
    - name: from_internal-dpl-clock_to_Dispatcher
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.internal-dpl-clock:from_internal-dpl-clock_to_Dispatcher"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    - name: from_readout-proxy_to_Dispatcher
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.readout-proxy:from_readout-proxy_to_Dispatcher"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: mft-full-qcmn-local-Dispatcher
  - name: "qc-task-MFT-QcMFTReadoutTask"
    connect:
    - name: from_internal-dpl-clock_to_qc-task-MFT-QcMFTReadoutTask
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.internal-dpl-clock:from_internal-dpl-clock_to_qc-task-MFT-QcMFTReadoutTask"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    - name: from_Dispatcher_to_qc-task-MFT-QcMFTReadoutTask
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.Dispatcher:from_Dispatcher_to_qc-task-MFT-QcMFTReadoutTask"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: mft-full-qcmn-local-qc-task-MFT-QcMFTReadoutTask
  - name: "mft-stf-decoder"
    connect:
    - name: from_Dispatcher_to_mft-stf-decoder
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.Dispatcher:from_Dispatcher_to_mft-stf-decoder"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: mft-full-qcmn-local-mft-stf-decoder
  - name: "qc-task-MFT-QcMFTDigitTask"
    connect:
    - name: from_internal-dpl-clock_to_qc-task-MFT-QcMFTDigitTask
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.internal-dpl-clock:from_internal-dpl-clock_to_qc-task-MFT-QcMFTDigitTask"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    - name: from_mft-stf-decoder_to_qc-task-MFT-QcMFTDigitTask
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.mft-stf-decoder:from_mft-stf-decoder_to_qc-task-MFT-QcMFTDigitTask"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: mft-full-qcmn-local-qc-task-MFT-QcMFTDigitTask
  - name: "QcMFTReadoutTask-proxy"
    connect:
    - name: from_qc-task-MFT-QcMFTReadoutTask_to_QcMFTReadoutTask-proxy
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.qc-task-MFT-QcMFTReadoutTask:from_qc-task-MFT-QcMFTReadoutTask_to_QcMFTReadoutTask-proxy"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    - name: QcMFTReadoutTask-proxy
      type: pub
      transport: zeromq
      target: "::QcMFTReadoutTask-proxy"
      rateLogging: "{{ fmq_rate_logging }}"
    task:
      load: mft-full-qcmn-local-QcMFTReadoutTask-proxy
  - name: "internal-dpl-injected-dummy-sink"
    connect:
    - name: from_mft-stf-decoder_to_internal-dpl-injected-dummy-sink
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.mft-stf-decoder:from_mft-stf-decoder_to_internal-dpl-injected-dummy-sink"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: mft-full-qcmn-local-internal-dpl-injected-dummy-sink
  - name: "QcMFTDigitTask-proxy"
    connect:
    - name: from_qc-task-MFT-QcMFTDigitTask_to_QcMFTDigitTask-proxy
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.qc-task-MFT-QcMFTDigitTask:from_qc-task-MFT-QcMFTDigitTask_to_QcMFTDigitTask-proxy"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    - name: QcMFTDigitTask-proxy
      type: pub
      transport: zeromq
      target: "::QcMFTDigitTask-proxy"
      rateLogging: "{{ fmq_rate_logging }}"
    task:
      load: mft-full-qcmn-local-QcMFTDigitTask-proxy
  - name: "dpl-output-proxy"
    connect:
    - name: from_readout-proxy_to_dpl-output-proxy
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.readout-proxy:from_readout-proxy_to_dpl-output-proxy"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    - name: from_qc-task-MFT-QcMFTReadoutTask_to_dpl-output-proxy
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.qc-task-MFT-QcMFTReadoutTask:from_qc-task-MFT-QcMFTReadoutTask_to_dpl-output-proxy"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    bind:
    - name: downstream
      type: push
      transport: shmem
      addressing: ipc
      rateLogging: "{{ fmq_rate_logging }}"
      global: "downstream-{{ it }}"
    task:
      load: mft-full-qcmn-local-dpl-output-proxy
