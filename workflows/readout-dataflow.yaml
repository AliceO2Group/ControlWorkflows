name: !public readout-dataflow
defaults:
  ###############################
  # Basic Configuration Panel
  ###############################
  dcs_enabled: !public # TODO: if false, DCS panel should be disabled
    value: "false"
    type: bool
    label: "DCS"
    description: "Enable/disable DCS SOR/EOR commands"
    widget: checkBox
    panel: BasicConfiguration
    index: -200
  dd_enabled: !public
    value: "true"
    type: bool
    label: "Data Distribution (FLP)"
    description: "Enable/disable Data Distribution components running on FLPs (StfBuilder and StfSender)"
    widget: checkBox
    panel: BasicConfiguration
    index: -100
  epn_enabled: !public # TODO: this should be the single var that enables both ODC and DD Scheduler
    value: "false"
    type: bool
    label: "EPN"
    description: "Enable/disable EPN"
    widget: checkBox
    panel: BasicConfiguration
  qc_remote_enabled: !public # TODO: if false, all per-detector remote QC workflows vars should be disabled
    value: "false"
    type: bool
    label: "QC remote"
    description: "Enable/disable QC remote"
    widget: checkBox
    panel: FLP_LocalProcessing
    index: -200
  cpv_dpl_workflow: !public
    value: "none"
    type: string
    label: "CPV FLP Local Processing/QC"
    description: "FLP DPL subworkflow to execute between StfBuilder and StfSender"
    widget: dropDownBox
    panel: FLP_LocalProcessing
    values:
      - none
      - cpv-compressor
      - cpv-qc-compressor
      - minimal-dpl
      - qc-daq
      - qcmn-daq-local
  cpv_qc_remote_workflow: !public
    value: "none"
    type: string
    label: "CPV QC remote workflow"
    description: "QC workflow to execute on QC servers"
    widget: dropDownBox
    panel: FLP_LocalProcessing
    values:
      - none
      - qcmn-daq-remote
  cpv_dcs_sor_parameters: !public
    value: "{}"
    type: string
    label: "CPV SOR parameters"
    description: "additional parameters for the DCS SOR"
    widget: editBox
    panel: DCS
  cpv_dcs_eor_parameters: !public
    value: "{}"
    type: string
    label: "CPV EOR parameters"
    description: "additional parameters for the DCS EOR"
    widget: editBox
    panel: DCS
  emc_dpl_workflow: !public
    value: "none"
    type: string
    label: "EMCAL FLP Local Processing/QC"
    description: "FLP DPL subworkflow to execute between StfBuilder and StfSender"
    widget: dropDownBox
    panel: FLP_LocalProcessing
    values:
      - none
      - minimal-dpl
      - qc-daq
      - qcmn-daq-local
  emc_qc_remote_workflow: !public
    value: "none"
    type: string
    label: "EMC QC remote workflow"
    description: "QC workflow to execute on QC servers"
    widget: dropDownBox
    panel: FLP_LocalProcessing
    values:
      - none
      - emc-qcmn-epn-remote
      - qcmn-daq-remote
  emc_dcs_sor_parameters: !public
    value: "{}"
    type: string
    label: "EMCAL SOR parameters"
    description: "additional parameters for the DCS SOR"
    widget: editBox
    panel: DCS
  emc_dcs_eor_parameters: !public
    value: "{}"
    type: string
    label: "EMCAL EOR parameters"
    description: "additional parameters for the DCS EOR"
    widget: editBox
    panel: DCS
  fdd_dpl_workflow: !public
    value: "none"
    type: string
    label: "FDD FLP Local Processing/QC"
    description: "FLP DPL subworkflow to execute between StfBuilder and StfSender"
    widget: dropDownBox
    panel: FLP_LocalProcessing
    values:
      - none
      - minimal-dpl
      - qc-daq
      - qcmn-daq-local
  fdd_qc_remote_workflow: !public
    value: "none"
    type: string
    label: "FDD QC remote workflow"
    description: "QC workflow to execute on QC servers"
    widget: dropDownBox
    panel: FLP_LocalProcessing
    values:
      - none
      - qcmn-daq-remote
  fdd_dcs_sor_parameters: !public
    value: "{}"
    type: string
    label: "FDD SOR parameters"
    description: "additional parameters for the DCS SOR"
    widget: editBox
    panel: DCS
  fdd_dcs_eor_parameters: !public
    value: "{}"
    type: string
    label: "FDD EOR parameters"
    description: "additional parameters for the DCS EOR"
    widget: editBox
    panel: DCS
  ft0_dpl_workflow: !public
    value: "none"
    type: string
    label: "FT0 FLP Local Processing/QC"
    description: "FLP DPL subworkflow to execute between StfBuilder and StfSender"
    widget: dropDownBox
    panel: FLP_LocalProcessing
    values:
      - none
      - ft0-digits-qc
      - minimal-dpl
      - qc-daq
      - qcmn-daq-local
  ft0_qc_remote_workflow: !public
    value: "none"
    type: string
    label: "FT0 QC remote workflow"
    description: "QC workflow to execute on QC servers"
    widget: dropDownBox
    panel: FLP_LocalProcessing
    values:
      - none
      - qcmn-daq-remote
  ft0_dcs_sor_parameters: !public
    value: "{}"
    type: string
    label: "FT0 SOR parameters"
    description: "additional parameters for the DCS SOR"
    widget: editBox
    panel: DCS
  ft0_dcs_eor_parameters: !public
    value: "{}"
    type: string
    label: "FT0 EOR parameters"
    description: "additional parameters for the DCS EOR"
    widget: editBox
    panel: DCS
  fv0_dpl_workflow: !public
    value: "none"
    type: string
    label: "FV0 FLP Local Processing/QC"
    description: "FLP DPL subworkflow to execute between StfBuilder and StfSender"
    widget: dropDownBox
    panel: FLP_LocalProcessing
    values:
      - none
      - minimal-dpl
      - qc-daq
      - qcmn-daq-local
  fv0_qc_remote_workflow: !public
    value: "none"
    type: string
    label: "FV0 QC remote workflow"
    description: "QC workflow to execute on QC servers"
    widget: dropDownBox
    panel: FLP_LocalProcessing
    values:
      - none
      - qcmn-daq-remote
  fv0_dcs_sor_parameters: !public
    value: "{}"
    type: string
    label: "FV0 SOR parameters"
    description: "additional parameters for the DCS SOR"
    widget: editBox
    panel: DCS
  fv0_dcs_eor_parameters: !public
    value: "{}"
    type: string
    label: "FV0 EOR parameters"
    description: "additional parameters for the DCS EOR"
    widget: editBox
    panel: DCS
  its_dpl_workflow: !public
    value: "none"
    type: string
    label: "ITS FLP Local Processing/QC"
    description: "FLP DPL subworkflow to execute between StfBuilder and StfSender"
    widget: dropDownBox
    panel: FLP_LocalProcessing
    values:
      - none
      - its-qc-fhr-fee
      - its-qcmn-fhr-fee-local
      - minimal-dpl
      - qc-daq
      - qcmn-daq-local
  its_qc_remote_workflow: !public
    value: "none"
    type: string
    label: "ITS QC remote workflow"
    description: "QC workflow to execute on QC servers"
    widget: dropDownBox
    panel: FLP_LocalProcessing
    values:
      - none
      - its-qcmn-fhr-fee-remote
      - qcmn-daq-remote
  its_dcs_sor_parameters: !public
    value: "{}"
    type: string
    label: "ITS SOR parameters"
    description: "additional parameters for the DCS SOR"
    widget: editBox
    panel: DCS
  its_dcs_eor_parameters: !public
    value: "{}"
    type: string
    label: "ITS EOR parameters"
    description: "additional parameters for the DCS EOR"
    widget: editBox
    panel: DCS
  hmp_dpl_workflow: !public
    value: "none"
    type: string
    label: "HMPID FLP Local Processing/QC"
    description: "FLP DPL subworkflow to execute between StfBuilder and StfSender"
    widget: dropDownBox
    panel: FLP_LocalProcessing
    values:
      - none
      - hmpid-raw-qc
      - hmpid-raw-qcmn-local
      - minimal-dpl
      - qc-daq
      - qcmn-daq-local
  hmp_qc_remote_workflow: !public
    value: "none"
    type: string
    label: "HMPID QC remote workflow"
    description: "QC workflow to execute on QC servers"
    widget: dropDownBox
    panel: FLP_LocalProcessing
    values:
      - none
      - hmpid-raw-qcmn-remote
      - qcmn-daq-remote
  hmp_dcs_sor_parameters: !public
    value: "{}"
    type: string
    label: "HMPID SOR parameters"
    description: "additional parameters for the DCS SOR"
    widget: editBox
    panel: DCS
  hmp_dcs_eor_parameters: !public
    value: "{}"
    type: string
    label: "HMPID EOR parameters"
    description: "additional parameters for the DCS EOR"
    widget: editBox
    panel: DCS
  mch_dpl_workflow: !public
    value: "none"
    type: string
    label: "MCH FLP Local Processing/QC"
    description: "FLP DPL subworkflow to execute between StfBuilder and StfSender"
    widget: dropDownBox
    panel: FLP_LocalProcessing
    values:
      - none
      - mch-qcmn-digits-local
      - minimal-dpl
      - qc-daq
      - qcmn-daq-local
  mch_qc_remote_workflow: !public
    value: "none"
    type: string
    label: "MCH QC remote workflow"
    description: "QC workflow to execute on QC servers"
    widget: dropDownBox
    panel: FLP_LocalProcessing
    values:
      - none
      - mch-qcmn-digits-remote
      - qcmn-daq-remote
  mch_dcs_sor_parameters: !public
    value: "{}"
    type: string
    label: "MCH SOR parameters"
    description: "additional parameters for the DCS SOR"
    widget: editBox
    panel: DCS
  mch_dcs_eor_parameters: !public
    value: "{}"
    type: string
    label: "MCH EOR parameters"
    description: "additional parameters for the DCS EOR"
    widget: editBox
    panel: DCS
  mft_dpl_workflow: !public
    value: "none"
    type: string
    label: "MFT FLP Local Processing/QC"
    description: "FLP DPL subworkflow to execute between StfBuilder and StfSender"
    widget: dropDownBox
    panel: FLP_LocalProcessing
    values:
      - none
      - mft-decoder
      - mft-digits-qc
      - mft-raw-direct-qcmn-local
      - mft-raw-qc
      - mft-raw-qcmn-local
      - minimal-dpl
      - qc-daq
      - qcmn-daq-local
  mft_qc_remote_workflow: !public
    value: "none"
    type: string
    label: "MFT QC remote workflow"
    description: "QC workflow to execute on QC servers"
    widget: dropDownBox
    panel: FLP_LocalProcessing
    values:
      - none
      - mft-raw-direct-qcmn-remote
      - mft-raw-qcmn-remote
      - qcmn-daq-remote
  mft_dcs_sor_parameters: !public
    value: "{}"
    type: string
    label: "MFT SOR parameters"
    description: "additional parameters for the DCS SOR"
    widget: editBox
    panel: DCS
  mft_dcs_eor_parameters: !public
    value: "{}"
    type: string
    label: "MFT EOR parameters"
    description: "additional parameters for the DCS EOR"
    widget: editBox
    panel: DCS
  mid_dpl_workflow: !public
    value: "none"
    type: string
    label: "MID FLP Local Processing/QC"
    description: "FLP DPL subworkflow to execute between StfBuilder and StfSender"
    widget: dropDownBox
    panel: FLP_LocalProcessing
    values:
      - none
      - mid-raw-parser
      - minimal-dpl
      - qc-daq
      - qcmn-daq-local
  mid_qc_remote_workflow: !public
    value: "none"
    type: string
    label: "MID QC remote workflow"
    description: "QC workflow to execute on QC servers"
    widget: dropDownBox
    panel: FLP_LocalProcessing
    values:
      - none
      - qcmn-daq-remote
  mid_dcs_sor_parameters: !public
    value: "{}"
    type: string
    label: "MID SOR parameters"
    description: "additional parameters for the DCS SOR"
    widget: editBox
    panel: DCS
  mid_dcs_eor_parameters: !public
    value: "{}"
    type: string
    label: "MID EOR parameters"
    description: "additional parameters for the DCS EOR"
    widget: editBox
    panel: DCS
  phs_dpl_workflow: !public
    value: "none"
    type: string
    label: "PHOS FLP Local Processing/QC"
    description: "FLP DPL subworkflow to execute between StfBuilder and StfSender"
    widget: dropDownBox
    panel: FLP_LocalProcessing
    values:
      - none
      - phos-compressor-raw-qc
      - phos-compressor-raw-qcmn-local
      - phos-compressor-raw-qcmnt3-local
      - phos-compressor-raw-qct3
      - phos-compressor
      - phos-compressort3
      - minimal-dpl
      - qc-daq
      - qcmn-daq-local
  phs_qc_remote_workflow: !public
    value: "none"
    type: string
    label: "PHOS QC remote workflow"
    description: "QC workflow to execute on QC servers"
    widget: dropDownBox
    panel: FLP_LocalProcessing
    values:
      - none
      - phos-compressor-raw-qcmn-remote
      - phos-compressor-raw-qcmnt3-remote
      - qcmn-daq-remote
  phs_dcs_sor_parameters: !public
    value: "{}"
    type: string
    label: "PHOS SOR parameters"
    description: "additional parameters for the DCS SOR"
    widget: editBox
    panel: DCS
  phs_dcs_eor_parameters: !public
    value: "{}"
    type: string
    label: "PHOS EOR parameters"
    description: "additional parameters for the DCS EOR"
    widget: editBox
    panel: DCS
  tof_dpl_workflow: !public
    value: "none"
    type: string
    label: "TOF FLP Local Processing/QC"
    description: "FLP DPL subworkflow to execute between StfBuilder and StfSender"
    widget: dropDownBox
    panel: FLP_LocalProcessing
    values:
      - none
      - tof-compressor
      - tof-full-qcmn-local
      - tof-qcmn-compressor-local
      - minimal-dpl
      - qc-daq
      - qcmn-daq-local
  tof_qc_remote_workflow: !public
    value: "none"
    type: string
    label: "TOF QC remote workflow"
    description: "QC workflow to execute on QC servers"
    widget: dropDownBox
    panel: FLP_LocalProcessing
    values:
      - none
      - qcmn-daq-remote
      - tof-full-qcmn-remote
      - tof-qcmn-compressor-remote
  tof_dcs_sor_parameters: !public
    value: "{}"
    type: string
    label: "TOF SOR parameters"
    description: "additional parameters for the DCS SOR"
    widget: editBox
    panel: DCS
  tof_dcs_eor_parameters: !public
    value: "{}"
    type: string
    label: "TOF EOR parameters"
    description: "additional parameters for the DCS EOR"
    widget: editBox
    panel: DCS
  tpc_dpl_workflow: !public
    value: "none"
    type: string
    label: "TPC FLP Local Processing/QC"
    description: "FLP DPL subworkflow to execute between StfBuilder and StfSender"
    widget: dropDownBox
    panel: FLP_LocalProcessing
    values:
      - none
      - minimal-dpl
      - qc-daq
      - qcmn-daq-local
  tpc_qc_remote_workflow: !public
    value: "none"
    type: string
    label: "TPC QC remote workflow"
    description: "QC workflow to execute on QC servers"
    widget: dropDownBox
    panel: FLP_LocalProcessing
    values:
      - none
      - qcmn-daq-remote
  tpc_dcs_sor_parameters: !public
    value: "{}"
    type: string
    label: "TPC SOR parameters"
    description: "additional parameters for the DCS SOR"
    widget: editBox
    panel: DCS
  tpc_dcs_eor_parameters: !public
    value: "{}"
    type: string
    label: "TPC EOR parameters"
    description: "additional parameters for the DCS EOR"
    widget: editBox
    panel: DCS
  trd_dpl_workflow: !public
    value: "none"
    type: string
    label: "TRD FLP Local Processing/QC"
    description: "FLP DPL subworkflow to execute between StfBuilder and StfSender"
    widget: dropDownBox
    panel: FLP_LocalProcessing
    values:
      - none
      - minimal-dpl
      - qc-daq
      - qcmn-daq-local
  trd_qc_remote_workflow: !public
    value: "none"
    type: string
    label: "TRD QC remote workflow"
    description: "QC workflow to execute on QC servers"
    widget: dropDownBox
    panel: FLP_LocalProcessing
    values:
      - none
      - qcmn-daq-remote
  trd_dcs_sor_parameters: !public
    value: "{}"
    type: string
    label: "TRD SOR parameters"
    description: "additional parameters for the DCS SOR"
    widget: editBox
    panel: DCS
  trd_dcs_eor_parameters: !public
    value: "{}"
    type: string
    label: "TRD EOR parameters"
    description: "additional parameters for the DCS EOR"
    widget: editBox
    panel: DCS
  zdc_dpl_workflow: !public
    value: "none"
    type: string
    label: "ZDC FLP Local Processing/QC"
    description: "FLP DPL subworkflow to execute between StfBuilder and StfSender"
    widget: dropDownBox
    panel: FLP_LocalProcessing
    values:
      - none
      - minimal-dpl
      - qc-daq
      - qcmn-daq-local
  zdc_qc_remote_workflow: !public
    value: "none"
    type: string
    label: "ZDC QC remote workflow"
    description: "QC workflow to execute on QC servers"
    widget: dropDownBox
    panel: FLP_LocalProcessing
    values:
      - none
      - qcmn-daq-remote
  zdc_dcs_sor_parameters: !public
    value: "{}"
    type: string
    label: "ZDC SOR parameters"
    description: "additional parameters for the DCS SOR"
    widget: editBox
    panel: DCS
  zdc_dcs_eor_parameters: !public
    value: "{}"
    type: string
    label: "ZDC EOR parameters"
    description: "additional parameters for the DCS EOR"
    widget: editBox
    panel: DCS
  ###############################
  # Cleanup Operations Panel
  ###############################      
  drop_caches_enabled: "true"
  drop_caches_timeout: "30s"
  fmq_initial_shm_cleanup_enabled: "true"
  fmq_initial_shm_cleanup_timeout: "30s"
  roc_cleanup_enabled: "true"
  fmq_cleanup_enabled: "true"
  #drop_caches_enabled: !public
  #  value: "true"
  #  type: bool
  #  label: "FLP cache cleanup"
  #  description: "Enable/disable deletion of system cache and page cache before CONFIGURE transition"
  #  widget: checkBox
  #  panel: CleanupOperations
  #fmq_initial_shm_cleanup_enabled: !public
  #  value: "true"
  #  type: bool
  #  label: "FLP SHM cleanup"
  #  description: "Enable/disable deletion of unused shared memory files before CONFIGURE transition"
  #  widget: checkBox
  #  panel: CleanupOperations
  #roc_cleanup_enabled: !public
  #  value: "true"
  #  type: bool
  #  label: "ReadoutCard cleanup on shutdown"
  #  description: "Enable/disable the ROC cleanup procedure on environment DESTROY"
  #  widget: checkBox
  #  panel: CleanupOperations
  #fmq_cleanup_enabled: !public
  #  value: "true"
  #  type: bool
  #  label: "FairMQ cleanup on shutdown"
  #  description: "Enable/disable the FairMQ shared memory cleanup procedure on environment DESTROY"
  #  widget: checkBox
  #  panel: CleanupOperations
  ###############################
  # Monitoring Panel
  ###############################
  monitoring_dpl_url: "no-op://"
  monitoring_qc_url: "no-op://"
  monitoring_dd_url: "no-op://"
  #monitoring_dpl_url:
  #  va.lue: "no-op://"
  #  type: string
  #  label: "DPL URL"
  #  description: "Monitoring URL for DPL"
  #  widget: editBox    
  #  panel: Monitoring    
  #monitoring_qc_url: !public 
  #  value: "no-op://"
  #  type: string
  #  label: "QC URL"
  #  description: "Monitoring URL for QC"
  #  widget: editBox    
  #  panel: Monitoring        
  #monitoring_dd_url: !public 
  #  value: "no-op://"
  #  type: string
  #  label: "Data Distribution URL"
  #  description: "Monitoring URL for Data Distribution"
  #  widget: editBox      
  #  panel: Monitoring            
  ###############################
  # Logging Panel
  ###############################
  infologger_severity: !public
    value: info
    type: string
    label: "DPL log level"
    description: "Minimum DPL logging severity which is sent to InfoLogger"
    widget: dropDownBox
    panel: Logging
    values:
      - "nolog"
      - "fatal"
      - "error"
      - "warn"
      - "state"
      - "info"
      - "debug"
      - "debug1"
      - "debug2"
      - "debug3"
      - "debug4"
      - "trace"
    index: 1 
  log_task_output: !public
    value: none
    type: string
    label: "Task stdout"
    description: "Select to where the standard output of controlled tasks should be forwarded to (select 'all' for InfoLogger)"
    widget: dropDownBox
    panel: Logging
    values:
      - "none"
      - "stdout"
      - "all"
    index: 2
  fmq_rate_logging: !public
    value: "10"
    type: number
    label: "FairMQ rate logging"
    description: "sets the interval at which FairMQ logs channel transfer rates"
    widget: editBox
    panel: Logging
    index: 3
  ###############################
  # EPN Panel
  ###############################        
  odc_topology: !public
    value: ""
    type: string
    label: "Topology"
    description: "DDS topology to be executed on EPNs"
    widget: editBox
    panel: EPN
    index: 1
  odc_resources: !public
    value: "{}"
    type: string
    label: "Resources"
    description: "EPN resources to be used"
    widget: editBox
    panel: EPN
    index: 2
  ###############################
  # Other variables
  ###############################
  stfb_standalone: "false"
  user: flp
  o2_install_path: "/opt/o2"
  o2_infologger_path: "/opt/o2-InfoLogger"    
  monitoring_readout_url: "no-op://" # TODO: is this used somewhere ? I could not find it anywhere in the workflows 
  qcdd_enabled: "false" # TODO FOR LATER: I think this one is no longer needed, we now enabled/disable the DAQ QC on FLP via dpl_workflow
  minimal_dpl_enabled: "false" # TODO FOR LATER: I think this one is no longer needed, we now enabled/disable the Minimal DPL workflow on FLP via dpl_workflow 
  roc_ctp_emulator_enabled: "false"
  odc_plugin: "epn"
  dpl_workflow: none
  detectors: "{{ DetectorsForHosts(hosts) }}"
vars:
  ddsched_enabled: "{{ epn_enabled == 'true' && dd_enabled == 'true' }}"
  odc_enabled: "{{ epn_enabled }}"
roles:
  - name: host-{{ it }}
    for:
      range: "{{ hosts }}"
      var: it
    vars:
      detector: "{{DetectorForHost( it )}}"
      readout_cfg_uri_standalone: "consul-ini://{{ consul_endpoint }}/o2/components/readout/ANY/any/readout-standalone-{{ it }}"
      readout_cfg_uri_stfb: "consul-ini://{{ consul_endpoint }}/o2/components/readout/ANY/any/readout-stfb-{{ it }}"
      dd_discovery_ib_hostname: "{{ it }}-ib" # MUST be defined for all stfb and stfs
      # dpl_workflow is set to <detector>_dpl_workflow if such an override exists
      dpl_workflow: "{{ PrefixedOverride( 'dpl_workflow', ToLower( DetectorForHost( it ) ) ) }}"
    constraints:
      - attribute: machine_id
        value: "{{ it }}"
    roles:
      - name: "readout"
        vars:
          readout_cfg_uri: '{{dd_enabled == "true" ? readout_cfg_uri_stfb : readout_cfg_uri_standalone}}'
        task:
          load: readout
      - name: "data-distribution"
        enabled: "{{dd_enabled == 'true' && (qcdd_enabled == 'false' && minimal_dpl_enabled == 'false' && dpl_workflow == 'none')}}"
        roles:
          - name: "stfb-standalone"
            enabled: "{{stfb_standalone}}"
            connect:
              - name: readout
                type: pull
                target: "{{ Up(2).Path }}.readout:readout"
                rateLogging: "{{ fmq_rate_logging }}"
            task:
              load: stfbuilder-nooutput
          - name: "stfb"
            enabled: "{{stfb_standalone == 'false'}}"
            vars:
              dd_discovery_stfb_id: stfb-{{ it }}-{{ NewID() }} # must be defined for all stfb roles
            connect:
              - name: readout
                type: pull
                target: "{{ Up(2).Path }}.readout:readout"
                rateLogging: "{{ fmq_rate_logging }}"
            task:
              load: stfbuilder-senderoutput
          - name: "stfs"
            enabled: "{{stfb_standalone == 'false'}}"
            vars:
              dd_discovery_stfs_id: stfs-{{ it }}-{{ NewID() }} # must be defined for all stfs roles
              stfs_input_channel_name: buildertosender
            connect:
              - name: buildertosender
                type: pull
                target: "{{ Parent().Path }}.stfb:buildertosender"
                rateLogging: "{{ fmq_rate_logging }}"
            task:
              load: stfsender
      - name: "data-distribution-dpl"
        enabled: "{{(qcdd_enabled == 'true' || minimal_dpl_enabled == 'true' || dpl_workflow != 'none') && dd_enabled == 'true'}}"
        defaults:
          path_to_readout_proxy: "{{ Parent().Path }}.data-distribution-dpl.stfb:dpl-chan"
        roles:
          - name: "stfb"
            enabled: "{{stfb_standalone == 'false'}}"
            vars:
              dd_discovery_stfb_id: stfb-{{ it }}-{{ NewID() }}
            connect:
              - name: readout
                type: pull
                target: "{{ Up(2).Path }}.readout:readout"
                rateLogging: "{{ fmq_rate_logging }}"
            bind:
              - name: dpl-chan
                type: push
                rateLogging: "{{ fmq_rate_logging }}"
                transport: shmem
                addressing: ipc
                sndBufSize: "4"
                global: "readout-proxy-{{ it }}"            
            task:
              #NOTE: plain stfbuilder TT (not stfbuilder-senderoutput) because we want dpl-chan
              load: stfbuilder
          - name: "stfs" # this stfs is for any DPL workflow. We assume that it has a device dpl-output-proxy and a downstream channel
            enabled: "{{stfb_standalone == 'false' && (qcdd_enabled == 'true' || minimal_dpl_enabled == 'true' || dpl_workflow != 'none')}}"
            vars:
              dd_discovery_stfs_id: stfs-{{ it }}-{{ NewID() }}
              stfs_input_channel_name: downstream
            connect:
              - name: downstream
                type: pull
                target: "::downstream-{{ it }}" # The subwf must provide this channel!
                rateLogging: "{{ fmq_rate_logging }}"
            task:
              load: stfsender
          - name: dpl
            enabled: "{{ qcdd_enabled == 'true' }}"
            include: qc-daq            
          - name: dpl
            enabled: "{{ minimal_dpl_enabled == 'true' }}"
            include: minimal-dpl
          - name: dpl
            enabled: "{{ dpl_workflow != 'none' }}"
            include: "{{ dpl_workflow }}"
      - name: o2-roc-ctp-emulators
        enabled: "{{roc_ctp_emulator_enabled == 'true'}}"
        defaults:
          roc_ctp_emulator_endpoints: '["#0"]'
        roles:
          - name: "endpoint-{{ endpoint_id }}"
            for:
              range: "{{roc_ctp_emulator_endpoints}}"
              var: endpoint_id
            roles:
              - name: o2-roc-ctp-emulator
                task:
                  load: "o2-roc-ctp-emulator"
                  trigger: enter_RUNNING
                  timeout: 10s
                  critical: false
      - name: drop-caches
        enabled: "{{drop_caches_enabled == 'true'}}"
        vars:
          shell_command: echo 3 > /proc/sys/vm/drop_caches && echo "system caches dropped" | O2_SYSTEM=ECS O2_PARTITION={{environment_id}} O2_ROLE={{it}} {{o2_infologger_path}}/bin/o2-infologger-log -x -s Info -o Facility=core/dropcaches
          user: root
        task:
          load: "shell-command"
          trigger: before_CONFIGURE
          timeout: "{{ drop_caches_timeout }}"
          critical: false # TODO: if this is set true, some basic task finished events aren't parsed correctly
      - name: fairmq-shmcleanup
        enabled: "{{fmq_initial_shm_cleanup_enabled == 'true'}}"
        vars:
          shell_command: "source /etc/profile.d/o2.sh && O2_PARTITION={{environment_id}} O2_ROLE={{it}} o2-aliecs-shmcleaner"
          user: root
        task:
          load: "shell-command"
          trigger: before_CONFIGURE
          timeout: "{{ fmq_initial_shm_cleanup_timeout }}"
          critical: false
      - name: fairmq-shmmonitor
        enabled: "{{fmq_cleanup_enabled == 'true'}}"
        task:
          load: "fairmq-shmmonitor"
          trigger: DESTROY
          timeout: 10s
          critical: false
      - name: o2-roc-cleanup
        enabled: "{{roc_cleanup_enabled == 'true'}}"
        task:
          load: "o2-roc-cleanup"
          trigger: DESTROY
          timeout: 10s
          critical: false
      - name: ps-aux-on-destroy
        vars:
          shell_command: ps aux|grep OCCPlugin
        task:
          load: "shell-command"
          trigger: DESTROY
          timeout: 10s
          critical: false
  - name: qc-servers
    enabled: "{{ qc_remote_enabled == 'true' }}"
    constraints:
      - attribute: machine_type
        value: qcserver
    defaults:
      qc_remote_workflow: none
    vars:
      user: qc
      session_id: "{{ environment_id }}" # we can't use "default" because other workflows might already exist on the same QC server
    roles:
      - name: qc-remote-workflow-{{ det }}
        for:
          range: "{{ detectors }}"
          var: det
        vars:
          qc_remote_workflow: "{{ PrefixedOverride( 'qc_remote_workflow', ToLower( det ) ) }}"
        roles:
          - name: dpl
            enabled: "{{ qc_remote_workflow != 'none' }}"
            include: "{{ qc_remote_workflow }}"
          - name: echo task
            enabled: "{{ qc_remote_workflow == 'none' }}"
            vars:
              shell_command: echo
            task:
              load: "shell-command"
  - name: dcs
    enabled: "{{dcs_enabled == 'true'}}"
    defaults:
    ###############################
    # DCS Panel
    ###############################
      dcs_detectors: "{{ detectors }}"
      dcs_sor_parameters: !public
        value: "{}"
        type: string
        label: "Global SOR parameters"
        description: "additional parameters for the DCS SOR"
        widget: editBox
        panel: DCS
        index: 2
      dcs_eor_parameters: !public
        value: "{}"
        type: string
        label: "Global EOR parameters"
        description: "additional parameters for the DCS EOR"
        widget: editBox
        panel: DCS
        index: 3
      run_type: !public # enum from AliECS common/runtype
        value: "NONE"
        type: string
        label: "Run Type"
        description: "Run type for DCS SOR/EOR"
        widget: comboBox
        panel: DCS
        values:
          - NONE
          - PHYSICS
          - TECHNICAL
          - PEDESTAL
          - PULSER
          - LASER
          - CALIBRATION
        index: 4
    roles:
      - name: sor      
        call:
          func: dcs.StartOfRun()
          trigger: enter_RUNNING
          timeout: 5s
          critical: true
      - name: eor
        call:
          func: dcs.EndOfRun()
          trigger: leave_RUNNING
          timeout: 5s
          critical: true
      - name: cleanup
        call:
          func: dcs.Cleanup()
          trigger: DESTROY
          timeout: 5s
          critical: true
  - name: dd-scheduler
    enabled: "{{ddsched_enabled == 'true'}}"
    roles:
      - name: initialize
        call:
          func: ddsched.PartitionInitialize()
          trigger: before_CONFIGURE
          await: after_CONFIGURE
          timeout: 5s
          critical: true
      - name: terminate
        call:
          func: ddsched.PartitionTerminate()
          trigger: after_RESET
          timeout: 5s
          critical: true
      - name: cleanup
        call:
          func: ddsched.EnsureTermination()
          trigger: DESTROY
          timeout: 5s
          critical: false
  - name: odc
    enabled: "{{odc_enabled == 'true'}}"
    roles:
      - name: configure
        call:
          func: odc.Configure()
          trigger: before_CONFIGURE
          await: after_CONFIGURE
          timeout: 30s
          critical: true
      - name: start
        call:
          func: odc.Start()
          trigger: before_START_ACTIVITY
          await: after_START_ACTIVITY
          timeout: 5s
          critical: true
      - name: stop
        call:
          func: odc.Stop()
          trigger: before_STOP_ACTIVITY
          await: after_STOP_ACTIVITY
          timeout: 5s
          critical: true
      - name: reset
        call:
          func: odc.Reset()
          trigger: before_RESET
          await: after_RESET
          timeout: 15s
          critical: true
      - name: cleanup
        call:
          func: odc.EnsureCleanup()
          trigger: DESTROY
          timeout: 5s
          critical: false
