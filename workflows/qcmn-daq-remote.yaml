name: qcmn-daq-remote
vars:
  dpl_command: >-
    o2-qc --config {{ qc_config_uri }} --remote -b
defaults:
  qc_remote_machine: "alio2-cr1-qme08"
  qc_config_uri: "consul-json://{{ consul_endpoint }}/o2/components/qc/ANY/any/qcmn-daq-test"
  monitoring_dpl_url: "no-op://"
  user: "flp"
  fmq_rate_logging: 0
  shm_segment_size: 10000000000
  shm_throw_bad_alloc: false
  session_id: default
  resources_monitoring: 15
constraints:
  - attribute: machine_id
    value: "{{ qc_remote_machine }}"
roles:
  - name: "internal-dpl-clock"
    connect:
    task:
      load: qcmn-daq-remote-internal-dpl-clock
  - name: "DAQ-MultiNodeLocal-proxy"
    connect:
    - name: from_internal-dpl-clock_to_DAQ-MultiNodeLocal-proxy
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.internal-dpl-clock:from_internal-dpl-clock_to_DAQ-MultiNodeLocal-proxy"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    bind:
    - name: DAQ-MultiNodeLocal-proxy
      type: sub
      transport: zeromq
      addressing: tcp
      rateLogging: "{{ fmq_rate_logging }}"
      global: "DAQ-MultiNodeLocal-proxy"
    task:
      load: qcmn-daq-remote-DAQ-MultiNodeLocal-proxy
  - name: "DAQ-TaskB-proxy"
    connect:
    - name: from_internal-dpl-clock_to_DAQ-TaskB-proxy
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.internal-dpl-clock:from_internal-dpl-clock_to_DAQ-TaskB-proxy"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    bind:
    - name: DAQ-TaskB-proxy
      type: sub
      transport: zeromq
      addressing: tcp
      rateLogging: "{{ fmq_rate_logging }}"
      target: "tcp://*:47749"
    task:
      load: qcmn-daq-remote-DAQ-TaskB-proxy
  - name: "DAQ-TaskC-proxy"
    connect:
    - name: from_internal-dpl-clock_to_DAQ-TaskC-proxy
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.internal-dpl-clock:from_internal-dpl-clock_to_DAQ-TaskC-proxy"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    bind:
    - name: DAQ-TaskC-proxy
      type: sub
      transport: zeromq
      addressing: tcp
      rateLogging: "{{ fmq_rate_logging }}"
      target: "tcp://*:47748"
    task:
      load: qcmn-daq-remote-DAQ-TaskC-proxy
  - name: "DAQ-TaskD-proxy"
    connect:
    - name: from_internal-dpl-clock_to_DAQ-TaskD-proxy
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.internal-dpl-clock:from_internal-dpl-clock_to_DAQ-TaskD-proxy"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    bind:
    - name: DAQ-TaskD-proxy
      type: sub
      transport: zeromq
      addressing: tcp
      rateLogging: "{{ fmq_rate_logging }}"
      target: "tcp://*:47747"
    task:
      load: qcmn-daq-remote-DAQ-TaskD-proxy
  - name: "DAQ-TaskE-proxy"
    connect:
    - name: from_internal-dpl-clock_to_DAQ-TaskE-proxy
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.internal-dpl-clock:from_internal-dpl-clock_to_DAQ-TaskE-proxy"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    bind:
    - name: DAQ-TaskE-proxy
      type: sub
      transport: zeromq
      addressing: tcp
      rateLogging: "{{ fmq_rate_logging }}"
      target: "tcp://*:47746"
    task:
      load: qcmn-daq-remote-DAQ-TaskE-proxy
  - name: "MERGER-MultiNodeLocal1l-0"
    connect:
    - name: from_internal-dpl-clock_to_MERGER-MultiNodeLocal1l-0
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.internal-dpl-clock:from_internal-dpl-clock_to_MERGER-MultiNodeLocal1l-0"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    - name: from_DAQ-MultiNodeLocal-proxy_to_MERGER-MultiNodeLocal1l-0
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.DAQ-MultiNodeLocal-proxy:from_DAQ-MultiNodeLocal-proxy_to_MERGER-MultiNodeLocal1l-0"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: qcmn-daq-remote-MERGER-MultiNodeLocal1l-0
  - name: "MERGER-TaskB1l-0"
    connect:
    - name: from_internal-dpl-clock_to_MERGER-TaskB1l-0
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.internal-dpl-clock:from_internal-dpl-clock_to_MERGER-TaskB1l-0"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    - name: from_DAQ-TaskB-proxy_to_MERGER-TaskB1l-0
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.DAQ-TaskB-proxy:from_DAQ-TaskB-proxy_to_MERGER-TaskB1l-0"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: qcmn-daq-remote-MERGER-TaskB1l-0
  - name: "MERGER-TaskC1l-0"
    connect:
    - name: from_internal-dpl-clock_to_MERGER-TaskC1l-0
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.internal-dpl-clock:from_internal-dpl-clock_to_MERGER-TaskC1l-0"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    - name: from_DAQ-TaskC-proxy_to_MERGER-TaskC1l-0
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.DAQ-TaskC-proxy:from_DAQ-TaskC-proxy_to_MERGER-TaskC1l-0"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: qcmn-daq-remote-MERGER-TaskC1l-0
  - name: "MERGER-TaskD1l-0"
    connect:
    - name: from_internal-dpl-clock_to_MERGER-TaskD1l-0
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.internal-dpl-clock:from_internal-dpl-clock_to_MERGER-TaskD1l-0"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    - name: from_DAQ-TaskD-proxy_to_MERGER-TaskD1l-0
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.DAQ-TaskD-proxy:from_DAQ-TaskD-proxy_to_MERGER-TaskD1l-0"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: qcmn-daq-remote-MERGER-TaskD1l-0
  - name: "MERGER-TaskE1l-0"
    connect:
    - name: from_internal-dpl-clock_to_MERGER-TaskE1l-0
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.internal-dpl-clock:from_internal-dpl-clock_to_MERGER-TaskE1l-0"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    - name: from_DAQ-TaskE-proxy_to_MERGER-TaskE1l-0
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.DAQ-TaskE-proxy:from_DAQ-TaskE-proxy_to_MERGER-TaskE1l-0"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: qcmn-daq-remote-MERGER-TaskE1l-0
  - name: "qc-check-DAQ-MultiNodeLocalCheck"
    connect:
    - name: from_MERGER-MultiNodeLocal1l-0_to_qc-check-DAQ-MultiNodeLocalCheck
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.MERGER-MultiNodeLocal1l-0:from_MERGER-MultiNodeLocal1l-0_to_qc-check-DAQ-MultiNodeLocalCheck"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: qcmn-daq-remote-qc-check-DAQ-MultiNodeLocalCheck
  - name: "qc-check-sink-QC_TaskB_0"
    connect:
    - name: from_MERGER-TaskB1l-0_to_qc-check-sink-QC_TaskB_0
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.MERGER-TaskB1l-0:from_MERGER-TaskB1l-0_to_qc-check-sink-QC_TaskB_0"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: qcmn-daq-remote-qc-check-sink-QC_TaskB_0
  - name: "qc-check-sink-QC_TaskC_0"
    connect:
    - name: from_MERGER-TaskC1l-0_to_qc-check-sink-QC_TaskC_0
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.MERGER-TaskC1l-0:from_MERGER-TaskC1l-0_to_qc-check-sink-QC_TaskC_0"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: qcmn-daq-remote-qc-check-sink-QC_TaskC_0
  - name: "qc-check-sink-QC_TaskD_0"
    connect:
    - name: from_MERGER-TaskD1l-0_to_qc-check-sink-QC_TaskD_0
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.MERGER-TaskD1l-0:from_MERGER-TaskD1l-0_to_qc-check-sink-QC_TaskD_0"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: qcmn-daq-remote-qc-check-sink-QC_TaskD_0
  - name: "qc-check-sink-QC_TaskE_0"
    connect:
    - name: from_MERGER-TaskE1l-0_to_qc-check-sink-QC_TaskE_0
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.MERGER-TaskE1l-0:from_MERGER-TaskE1l-0_to_qc-check-sink-QC_TaskE_0"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: qcmn-daq-remote-qc-check-sink-QC_TaskE_0
  - name: "internal-dpl-injected-dummy-sink"
    connect:
    - name: from_qc-check-DAQ-MultiNodeLocalCheck_to_internal-dpl-injected-dummy-sink
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.qc-check-DAQ-MultiNodeLocalCheck:from_qc-check-DAQ-MultiNodeLocalCheck_to_internal-dpl-injected-dummy-sink"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: qcmn-daq-remote-internal-dpl-injected-dummy-sink
  - name: fairmq-shmmonitor
    enabled: "{{fmq_cleanup_enabled == 'true'}}"
    task:
      load: "fairmq-shmmonitor"
      trigger: DESTROY
      timeout: 10s
      critical: false
