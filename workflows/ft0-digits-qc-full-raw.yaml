name: ft0-digits-qc-full-raw
vars:
  dpl_command: >-
    o2-dpl-raw-proxy -b --session default --dataspec 'rawdata:FT0/RAWDATA;dd:FLP/DISTSUBTIMEFRAME/0' --readout-proxy '--channel-config "name=readout-proxy,type=pull,method=connect,address=ipc:///tmp/stf-builder-dpl-pipe-0,transport=shmem,rateLogging=10"' --pipeline readout-proxy:1 | o2-ft0-flp-dpl-workflow -b --session default --output-dir=/tmp --nevents 10000 --pipeline ft0-datareader-dpl:1 | o2-datasampling-standalone -b --session default --config {{ ds_config_uri }} | o2-calibration-ft0-tf-processor -b --session default --dispatcher-mode | o2-calibration-ft0-calibration -b --session default | o2-calibration-ccdb-populator-workflow -b --session default --ccdb-path=http://ccdb-test.cern.ch:8080 | o2-dpl-output-proxy -b --session default --dataspec 'rawdata:FT0/RAWDATA;digits:FT0/DIGITSBC/0;channels:FT0/DIGITSCH/0;dd:FLP/DISTSUBTIMEFRAME/0' --dpl-output-proxy '--channel-config "name=downstream,type=push,method=bind,address=ipc:///tmp/stf-pipe-0,rateLogging=10,transport=shmem"' | o2-qc --config {{ qc_config_uri }} -b
defaults:
  ds_config_uri: "consul-json://{{ consul_endpoint }}/o2/components/qc/ANY/any/ft0-digits-datasampling-{{ it }}"
  qc_config_uri: "consul-json://{{ consul_endpoint }}/o2/components/qc/ANY/any/ft0-digits-qc-full-{{ it }}"
  monitoring_dpl_url: "no-op://"
  user: "flp"
  fmq_rate_logging: 0
  shm_segment_size: 10000000000
  shm_throw_bad_alloc: false
  session_id: default
  resources_monitoring: 15
roles:
  - name: "internal-dpl-clock"
    connect:
    task:
      load: ft0-digits-qc-full-raw-internal-dpl-clock
  - name: "readout-proxy"
    connect:
    - name: from_internal-dpl-clock_to_readout-proxy
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.internal-dpl-clock:from_internal-dpl-clock_to_readout-proxy"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    - name: readout-proxy
      type: pull
      transport: shmem
      target: "::readout-proxy-{{ it }}"
      rateLogging: "{{ fmq_rate_logging }}"
    task:
      load: ft0-digits-qc-full-raw-readout-proxy
  - name: "PP-TASK-RUNNER-BasicPPTask"
    connect:
    - name: from_internal-dpl-clock_to_PP-TASK-RUNNER-BasicPPTask
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.internal-dpl-clock:from_internal-dpl-clock_to_PP-TASK-RUNNER-BasicPPTask"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: ft0-digits-qc-full-raw-PP-TASK-RUNNER-BasicPPTask
  - name: "PP-TASK-RUNNER-OutOfBunchCollTask"
    connect:
    - name: from_internal-dpl-clock_to_PP-TASK-RUNNER-OutOfBunchCollTask
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.internal-dpl-clock:from_internal-dpl-clock_to_PP-TASK-RUNNER-OutOfBunchCollTask"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: ft0-digits-qc-full-raw-PP-TASK-RUNNER-OutOfBunchCollTask
  - name: "PP-TASK-RUNNER-TrendPlusPlus"
    connect:
    - name: from_internal-dpl-clock_to_PP-TASK-RUNNER-TrendPlusPlus
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.internal-dpl-clock:from_internal-dpl-clock_to_PP-TASK-RUNNER-TrendPlusPlus"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: ft0-digits-qc-full-raw-PP-TASK-RUNNER-TrendPlusPlus
  - name: "ft0-datareader-dpl"
    connect:
    - name: from_readout-proxy_to_ft0-datareader-dpl
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.readout-proxy:from_readout-proxy_to_ft0-datareader-dpl"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: ft0-digits-qc-full-raw-ft0-datareader-dpl
  - name: "QC-CHECK-RUNNER-OutOfBunchCollCheck"
    connect:
    - name: from_PP-TASK-RUNNER-OutOfBunchCollTask_to_QC-CHECK-RUNNER-OutOfBunchCollCheck
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.PP-TASK-RUNNER-OutOfBunchCollTask:from_PP-TASK-RUNNER-OutOfBunchCollTask_to_QC-CHECK-RUNNER-OutOfBunchCollCheck"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: ft0-digits-qc-full-raw-QC-CHECK-RUNNER-OutOfBunchCollCheck
  - name: "FT0DigitWriterRaw"
    connect:
    - name: from_ft0-datareader-dpl_to_FT0DigitWriterRaw
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.ft0-datareader-dpl:from_ft0-datareader-dpl_to_FT0DigitWriterRaw"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: ft0-digits-qc-full-raw-FT0DigitWriterRaw
  - name: "Dispatcher"
    connect:
    - name: from_internal-dpl-clock_to_Dispatcher
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.internal-dpl-clock:from_internal-dpl-clock_to_Dispatcher"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    - name: from_FT0DigitWriterRaw_to_Dispatcher
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.FT0DigitWriterRaw:from_FT0DigitWriterRaw_to_Dispatcher"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: ft0-digits-qc-full-raw-Dispatcher
  - name: "FT0TFProcessor"
    connect:
    - name: from_Dispatcher_to_FT0TFProcessor
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.Dispatcher:from_Dispatcher_to_FT0TFProcessor"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: ft0-digits-qc-full-raw-FT0TFProcessor
  - name: "QC-TASK-RUNNER-CalibrationTask"
    connect:
    - name: from_internal-dpl-clock_to_QC-TASK-RUNNER-CalibrationTask
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.internal-dpl-clock:from_internal-dpl-clock_to_QC-TASK-RUNNER-CalibrationTask"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    - name: from_FT0TFProcessor_to_QC-TASK-RUNNER-CalibrationTask
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.FT0TFProcessor:from_FT0TFProcessor_to_QC-TASK-RUNNER-CalibrationTask"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: ft0-digits-qc-full-raw-QC-TASK-RUNNER-CalibrationTask
  - name: "QC-TASK-RUNNER-DigitQcTask"
    connect:
    - name: from_internal-dpl-clock_to_QC-TASK-RUNNER-DigitQcTask
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.internal-dpl-clock:from_internal-dpl-clock_to_QC-TASK-RUNNER-DigitQcTask"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    - name: from_QC-TASK-RUNNER-CalibrationTask_to_QC-TASK-RUNNER-DigitQcTask
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.QC-TASK-RUNNER-CalibrationTask:from_QC-TASK-RUNNER-CalibrationTask_to_QC-TASK-RUNNER-DigitQcTask"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: ft0-digits-qc-full-raw-QC-TASK-RUNNER-DigitQcTask
  - name: "QC-TASK-RUNNER-TriggerQcTask"
    connect:
    - name: from_internal-dpl-clock_to_QC-TASK-RUNNER-TriggerQcTask
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.internal-dpl-clock:from_internal-dpl-clock_to_QC-TASK-RUNNER-TriggerQcTask"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    - name: from_QC-TASK-RUNNER-DigitQcTask_to_QC-TASK-RUNNER-TriggerQcTask
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.QC-TASK-RUNNER-DigitQcTask:from_QC-TASK-RUNNER-DigitQcTask_to_QC-TASK-RUNNER-TriggerQcTask"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: ft0-digits-qc-full-raw-QC-TASK-RUNNER-TriggerQcTask
  - name: "calib-ft0-channel-time"
    connect:
    - name: from_FT0TFProcessor_to_calib-ft0-channel-time
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.FT0TFProcessor:from_FT0TFProcessor_to_calib-ft0-channel-time"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: ft0-digits-qc-full-raw-calib-ft0-channel-time
  - name: "QC-CHECK-RUNNER-CFDEffCheck"
    connect:
    - name: from_QC-TASK-RUNNER-DigitQcTask_to_QC-CHECK-RUNNER-CFDEffCheck
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.QC-TASK-RUNNER-DigitQcTask:from_QC-TASK-RUNNER-DigitQcTask_to_QC-CHECK-RUNNER-CFDEffCheck"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: ft0-digits-qc-full-raw-QC-CHECK-RUNNER-CFDEffCheck
  - name: "QC-CHECK-RUNNER-CalibratedChannelTimeCheck"
    connect:
    - name: from_QC-TASK-RUNNER-CalibrationTask_to_QC-CHECK-RUNNER-CalibratedChannelTimeCheck
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.QC-TASK-RUNNER-CalibrationTask:from_QC-TASK-RUNNER-CalibrationTask_to_QC-CHECK-RUNNER-CalibratedChannelTimeCheck"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: ft0-digits-qc-full-raw-QC-CHECK-RUNNER-CalibratedChannelTimeCheck
  - name: "QC-CHECK-RUNNER-sink-QC_BasicPPTask-mo_0"
    connect:
    - name: from_PP-TASK-RUNNER-BasicPPTask_to_QC-CHECK-RUNNER-sink-QC_BasicPPTask-mo_0
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.PP-TASK-RUNNER-BasicPPTask:from_PP-TASK-RUNNER-BasicPPTask_to_QC-CHECK-RUNNER-sink-QC_BasicPPTask-mo_0"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: ft0-digits-qc-full-raw-QC-CHECK-RUNNER-sink-QC_BasicPPTask-mo_0
  - name: "QC-CHECK-RUNNER-sink-QC_TrendPlusPlus-mo_0"
    connect:
    - name: from_PP-TASK-RUNNER-TrendPlusPlus_to_QC-CHECK-RUNNER-sink-QC_TrendPlusPlus-mo_0
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.PP-TASK-RUNNER-TrendPlusPlus:from_PP-TASK-RUNNER-TrendPlusPlus_to_QC-CHECK-RUNNER-sink-QC_TrendPlusPlus-mo_0"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: ft0-digits-qc-full-raw-QC-CHECK-RUNNER-sink-QC_TrendPlusPlus-mo_0
  - name: "QC-CHECK-RUNNER-sink-QC_TriggerQcTask-mo_0"
    connect:
    - name: from_QC-TASK-RUNNER-TriggerQcTask_to_QC-CHECK-RUNNER-sink-QC_TriggerQcTask-mo_0
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.QC-TASK-RUNNER-TriggerQcTask:from_QC-TASK-RUNNER-TriggerQcTask_to_QC-CHECK-RUNNER-sink-QC_TriggerQcTask-mo_0"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: ft0-digits-qc-full-raw-QC-CHECK-RUNNER-sink-QC_TriggerQcTask-mo_0
  - name: "ccdb-populator"
    connect:
    - name: from_calib-ft0-channel-time_to_ccdb-populator
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.calib-ft0-channel-time:from_calib-ft0-channel-time_to_ccdb-populator"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: ft0-digits-qc-full-raw-ccdb-populator
  - name: "internal-dpl-injected-dummy-sink"
    connect:
    - name: from_QC-CHECK-RUNNER-OutOfBunchCollCheck_to_internal-dpl-injected-dummy-sink
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.QC-CHECK-RUNNER-OutOfBunchCollCheck:from_QC-CHECK-RUNNER-OutOfBunchCollCheck_to_internal-dpl-injected-dummy-sink"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    - name: from_QC-CHECK-RUNNER-CFDEffCheck_to_internal-dpl-injected-dummy-sink
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.QC-CHECK-RUNNER-CFDEffCheck:from_QC-CHECK-RUNNER-CFDEffCheck_to_internal-dpl-injected-dummy-sink"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    - name: from_QC-CHECK-RUNNER-CalibratedChannelTimeCheck_to_internal-dpl-injected-dummy-sink
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.QC-CHECK-RUNNER-CalibratedChannelTimeCheck:from_QC-CHECK-RUNNER-CalibratedChannelTimeCheck_to_internal-dpl-injected-dummy-sink"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: ft0-digits-qc-full-raw-internal-dpl-injected-dummy-sink
  - name: "dpl-output-proxy"
    connect:
    - name: from_ft0-datareader-dpl_to_dpl-output-proxy
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.ft0-datareader-dpl:from_ft0-datareader-dpl_to_dpl-output-proxy"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    - name: from_Dispatcher_to_dpl-output-proxy
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.Dispatcher:from_Dispatcher_to_dpl-output-proxy"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    bind:
    - name: downstream
      type: push
      transport: shmem
      addressing: ipc
      rateLogging: "{{ fmq_rate_logging }}"
      global: "downstream-{{ it }}"
    task:
      load: ft0-digits-qc-full-raw-dpl-output-proxy
