name: tpc-calib-full
roles:
  - name: tpc-idc-simple
    enabled: "{{ it != 'alio2-cr1-flp145' }}"
    include: tpc-idc-simple
  - name: tpc-sac
    enabled: "{{ it == 'alio2-cr1-flp145' }}"
    include: minimal-dpl
