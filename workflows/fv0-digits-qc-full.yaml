name: fv0-digits-qc-full
vars:
  dpl_command: >-
    o2-dpl-raw-proxy -b --session default --dataspec 'A1:FV0/RAWDATA;dd:FLP/DISTSUBTIMEFRAME/0' --readout-proxy '--channel-config "name=readout-proxy,type=pull,method=connect,address=ipc:///tmp/stf-builder-dpl-pipe-0,transport=shmem,rateLogging=10"' --pipeline readout-proxy:1 | o2-fv0-flp-dpl-workflow -b --session default --output-dir=/tmp --nevents 10000 --pipeline fv0-datareader-dpl:1 --configKeyValues 'NameConf.mCCDBServer=http://127.0.0.1:8084;' | o2-datasampling-standalone -b --session default --config {{ ds_config_uri }} | o2-calibration-fv0-tf-processor -b --session default --dispatcher-mode | o2-calibration-fv0-channel-offset-calibration -b --session default --tf-per-slot 56000 | o2-calibration-ccdb-populator-workflow -b --session default --ccdb-path=http://ccdb-test.cern.ch:8080 | o2-dpl-output-proxy -b --session default --dataspec 'digits:FV0/DIGITSBC/0;channels:FV0/DIGITSCH/0;dd:FLP/DISTSUBTIMEFRAME/0' --dpl-output-proxy '--channel-config "name=downstream,type=push,method=bind,address=ipc:///tmp/stf-pipe-0,rateLogging=10,transport=shmem"' | o2-qc --config {{ qc_config_uri }} -b
defaults:
  ds_config_uri: "consul-json://{{ consul_endpoint }}/o2/components/qc/ANY/any/fv0-digits-datasampling-{{ it }}"
  qc_config_uri: "consul-json://{{ consul_endpoint }}/o2/components/qc/ANY/any/fv0-digits-qc-full-{{ it }}"
  monitoring_dpl_url: "no-op://"
  user: "flp"
  fmq_rate_logging: 0
  shm_segment_size: 10000000000
  shm_throw_bad_alloc: false
  session_id: default
  resources_monitoring: 15
roles:
  - name: "internal-dpl-clock"
    connect:
    task:
      load: fv0-digits-qc-full-internal-dpl-clock
  - name: "readout-proxy"
    connect:
    - name: from_internal-dpl-clock_to_readout-proxy
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.internal-dpl-clock:from_internal-dpl-clock_to_readout-proxy"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    - name: readout-proxy
      type: pull
      transport: shmem
      target: "::readout-proxy-{{ it }}"
      rateLogging: "{{ fmq_rate_logging }}"
    task:
      load: fv0-digits-qc-full-readout-proxy
  - name: "PP-TASK-RUNNER-OutOfBunchCollTask"
    connect:
    - name: from_internal-dpl-clock_to_PP-TASK-RUNNER-OutOfBunchCollTask
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.internal-dpl-clock:from_internal-dpl-clock_to_PP-TASK-RUNNER-OutOfBunchCollTask"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: fv0-digits-qc-full-PP-TASK-RUNNER-OutOfBunchCollTask
  - name: "PP-TASK-RUNNER-PostProcTask"
    connect:
    - name: from_internal-dpl-clock_to_PP-TASK-RUNNER-PostProcTask
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.internal-dpl-clock:from_internal-dpl-clock_to_PP-TASK-RUNNER-PostProcTask"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: fv0-digits-qc-full-PP-TASK-RUNNER-PostProcTask
  - name: "PP-TASK-RUNNER-TrendingTask"
    connect:
    - name: from_internal-dpl-clock_to_PP-TASK-RUNNER-TrendingTask
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.internal-dpl-clock:from_internal-dpl-clock_to_PP-TASK-RUNNER-TrendingTask"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: fv0-digits-qc-full-PP-TASK-RUNNER-TrendingTask
  - name: "fv0-datareader-dpl"
    connect:
    - name: from_readout-proxy_to_fv0-datareader-dpl
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.readout-proxy:from_readout-proxy_to_fv0-datareader-dpl"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: fv0-digits-qc-full-fv0-datareader-dpl
  - name: "qc-check-FV0OutOfBunchCollCheck"
    connect:
    - name: from_PP-TASK-RUNNER-OutOfBunchCollTask_to_qc-check-FV0OutOfBunchCollCheck
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.PP-TASK-RUNNER-OutOfBunchCollTask:from_PP-TASK-RUNNER-OutOfBunchCollTask_to_qc-check-FV0OutOfBunchCollCheck"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: fv0-digits-qc-full-qc-check-FV0OutOfBunchCollCheck
  - name: "FV0DigitWriterRaw"
    connect:
    - name: from_fv0-datareader-dpl_to_FV0DigitWriterRaw
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.fv0-datareader-dpl:from_fv0-datareader-dpl_to_FV0DigitWriterRaw"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: fv0-digits-qc-full-FV0DigitWriterRaw
  - name: "Dispatcher"
    connect:
    - name: from_internal-dpl-clock_to_Dispatcher
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.internal-dpl-clock:from_internal-dpl-clock_to_Dispatcher"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    - name: from_FV0DigitWriterRaw_to_Dispatcher
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.FV0DigitWriterRaw:from_FV0DigitWriterRaw_to_Dispatcher"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: fv0-digits-qc-full-Dispatcher
  - name: "FV0TFProcessor"
    connect:
    - name: from_Dispatcher_to_FV0TFProcessor
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.Dispatcher:from_Dispatcher_to_FV0TFProcessor"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: fv0-digits-qc-full-FV0TFProcessor
  - name: "qc-task-FV0-CalibrationTask"
    connect:
    - name: from_internal-dpl-clock_to_qc-task-FV0-CalibrationTask
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.internal-dpl-clock:from_internal-dpl-clock_to_qc-task-FV0-CalibrationTask"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    - name: from_FV0TFProcessor_to_qc-task-FV0-CalibrationTask
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.FV0TFProcessor:from_FV0TFProcessor_to_qc-task-FV0-CalibrationTask"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: fv0-digits-qc-full-qc-task-FV0-CalibrationTask
  - name: "qc-task-FV0-DigitQcTask"
    connect:
    - name: from_internal-dpl-clock_to_qc-task-FV0-DigitQcTask
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.internal-dpl-clock:from_internal-dpl-clock_to_qc-task-FV0-DigitQcTask"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    - name: from_qc-task-FV0-CalibrationTask_to_qc-task-FV0-DigitQcTask
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.qc-task-FV0-CalibrationTask:from_qc-task-FV0-CalibrationTask_to_qc-task-FV0-DigitQcTask"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: fv0-digits-qc-full-qc-task-FV0-DigitQcTask
  - name: "qc-task-FV0-TriggerQcTask"
    connect:
    - name: from_internal-dpl-clock_to_qc-task-FV0-TriggerQcTask
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.internal-dpl-clock:from_internal-dpl-clock_to_qc-task-FV0-TriggerQcTask"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    - name: from_qc-task-FV0-DigitQcTask_to_qc-task-FV0-TriggerQcTask
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.qc-task-FV0-DigitQcTask:from_qc-task-FV0-DigitQcTask_to_qc-task-FV0-TriggerQcTask"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: fv0-digits-qc-full-qc-task-FV0-TriggerQcTask
  - name: "calib-fv0-channel-time"
    connect:
    - name: from_FV0TFProcessor_to_calib-fv0-channel-time
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.FV0TFProcessor:from_FV0TFProcessor_to_calib-fv0-channel-time"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: fv0-digits-qc-full-calib-fv0-channel-time
  - name: "qc-check-FV0CFDEffCheck"
    connect:
    - name: from_qc-task-FV0-DigitQcTask_to_qc-check-FV0CFDEffCheck
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.qc-task-FV0-DigitQcTask:from_qc-task-FV0-DigitQcTask_to_qc-check-FV0CFDEffCheck"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: fv0-digits-qc-full-qc-check-FV0CFDEffCheck
  - name: "qc-check-sink-QC_PostProcTask-mo_0"
    connect:
    - name: from_PP-TASK-RUNNER-PostProcTask_to_qc-check-sink-QC_PostProcTask-mo_0
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.PP-TASK-RUNNER-PostProcTask:from_PP-TASK-RUNNER-PostProcTask_to_qc-check-sink-QC_PostProcTask-mo_0"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: fv0-digits-qc-full-qc-check-sink-QC_PostProcTask-mo_0
  - name: "qc-check-sink-QC_TrendingTask-mo_0"
    connect:
    - name: from_PP-TASK-RUNNER-TrendingTask_to_qc-check-sink-QC_TrendingTask-mo_0
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.PP-TASK-RUNNER-TrendingTask:from_PP-TASK-RUNNER-TrendingTask_to_qc-check-sink-QC_TrendingTask-mo_0"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: fv0-digits-qc-full-qc-check-sink-QC_TrendingTask-mo_0
  - name: "qc-check-sink-QC_CalibrationTask_0"
    connect:
    - name: from_qc-task-FV0-CalibrationTask_to_qc-check-sink-QC_CalibrationTask_0
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.qc-task-FV0-CalibrationTask:from_qc-task-FV0-CalibrationTask_to_qc-check-sink-QC_CalibrationTask_0"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: fv0-digits-qc-full-qc-check-sink-QC_CalibrationTask_0
  - name: "qc-check-sink-QC_TriggerQcTask_0"
    connect:
    - name: from_qc-task-FV0-TriggerQcTask_to_qc-check-sink-QC_TriggerQcTask_0
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.qc-task-FV0-TriggerQcTask:from_qc-task-FV0-TriggerQcTask_to_qc-check-sink-QC_TriggerQcTask_0"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: fv0-digits-qc-full-qc-check-sink-QC_TriggerQcTask_0
  - name: "ccdb-populator"
    connect:
    - name: from_calib-fv0-channel-time_to_ccdb-populator
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.calib-fv0-channel-time:from_calib-fv0-channel-time_to_ccdb-populator"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: fv0-digits-qc-full-ccdb-populator
  - name: "internal-dpl-injected-dummy-sink"
    connect:
    - name: from_qc-check-FV0OutOfBunchCollCheck_to_internal-dpl-injected-dummy-sink
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.qc-check-FV0OutOfBunchCollCheck:from_qc-check-FV0OutOfBunchCollCheck_to_internal-dpl-injected-dummy-sink"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    - name: from_qc-check-FV0CFDEffCheck_to_internal-dpl-injected-dummy-sink
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.qc-check-FV0CFDEffCheck:from_qc-check-FV0CFDEffCheck_to_internal-dpl-injected-dummy-sink"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: fv0-digits-qc-full-internal-dpl-injected-dummy-sink
  - name: "dpl-output-proxy"
    connect:
    - name: from_fv0-datareader-dpl_to_dpl-output-proxy
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.fv0-datareader-dpl:from_fv0-datareader-dpl_to_dpl-output-proxy"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    - name: from_Dispatcher_to_dpl-output-proxy
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.Dispatcher:from_Dispatcher_to_dpl-output-proxy"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    bind:
    - name: downstream
      type: push
      transport: shmem
      addressing: ipc
      rateLogging: "{{ fmq_rate_logging }}"
      global: "downstream-{{ it }}"
    task:
      load: fv0-digits-qc-full-dpl-output-proxy
