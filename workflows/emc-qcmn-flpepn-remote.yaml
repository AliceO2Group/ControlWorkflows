name: emc-qcmn-flpepn-remote
vars:
  dpl_command: >-
    o2-qc --config {{ qc_config_uri }} --remote -b
defaults:
  qc_remote_machine: "alio2-cr1-qc02"
  qc_config_uri: "consul-json://{{ consul_endpoint }}/o2/components/qc/ANY/any/emc-qcmn-flpepn"
  monitoring_dpl_url: "no-op://"
  user: "flp"
  fmq_rate_logging: 0
  shm_segment_size: 10000000000
  shm_throw_bad_alloc: false
  session_id: default
  resources_monitoring: 15
constraints:
  - attribute: machine_id
    value: "{{ qc_remote_machine }}"
roles:
  - name: "internal-dpl-clock"
    connect:
    task:
      load: emc-qcmn-flpepn-remote-internal-dpl-clock
  - name: "EMC-CellTask-proxy"
    connect:
    - name: from_internal-dpl-clock_to_EMC-CellTask-proxy
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.internal-dpl-clock:from_internal-dpl-clock_to_EMC-CellTask-proxy"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    bind:
    - name: EMC-CellTask-proxy
      type: sub
      transport: zeromq
      addressing: tcp
      rateLogging: "{{ fmq_rate_logging }}"
      target: "tcp://*:47702"
    task:
      load: emc-qcmn-flpepn-remote-EMC-CellTask-proxy
  - name: "EMC-RawTask-proxy"
    connect:
    - name: from_internal-dpl-clock_to_EMC-RawTask-proxy
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.internal-dpl-clock:from_internal-dpl-clock_to_EMC-RawTask-proxy"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    bind:
    - name: EMC-RawTask-proxy
      type: sub
      transport: zeromq
      addressing: tcp
      rateLogging: "{{ fmq_rate_logging }}"
      global: "EMC-RawTask-proxy"
    task:
      load: emc-qcmn-flpepn-remote-EMC-RawTask-proxy
  - name: "MERGER-CellTask1l-0"
    connect:
    - name: from_internal-dpl-clock_to_MERGER-CellTask1l-0
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.internal-dpl-clock:from_internal-dpl-clock_to_MERGER-CellTask1l-0"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    - name: from_EMC-CellTask-proxy_to_MERGER-CellTask1l-0
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.EMC-CellTask-proxy:from_EMC-CellTask-proxy_to_MERGER-CellTask1l-0"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: emc-qcmn-flpepn-remote-MERGER-CellTask1l-0
  - name: "MERGER-RawTask1l-0"
    connect:
    - name: from_internal-dpl-clock_to_MERGER-RawTask1l-0
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.internal-dpl-clock:from_internal-dpl-clock_to_MERGER-RawTask1l-0"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    - name: from_EMC-RawTask-proxy_to_MERGER-RawTask1l-0
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.EMC-RawTask-proxy:from_EMC-RawTask-proxy_to_MERGER-RawTask1l-0"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: emc-qcmn-flpepn-remote-MERGER-RawTask1l-0
  - name: "qc-check-DET-bY90"
    connect:
    - name: from_MERGER-RawTask1l-0_to_qc-check-DET-bY90
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.MERGER-RawTask1l-0:from_MERGER-RawTask1l-0_to_qc-check-DET-bY90"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: emc-qcmn-flpepn-remote-qc-check-DET-bY90
  - name: "qc-check-EMC-CellCheckAmplitude"
    connect:
    - name: from_MERGER-CellTask1l-0_to_qc-check-EMC-CellCheckAmplitude
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.MERGER-CellTask1l-0:from_MERGER-CellTask1l-0_to_qc-check-EMC-CellCheckAmplitude"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: emc-qcmn-flpepn-remote-qc-check-EMC-CellCheckAmplitude
  - name: "internal-dpl-injected-dummy-sink"
    connect:
    - name: from_qc-check-DET-bY90_to_internal-dpl-injected-dummy-sink
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.qc-check-DET-bY90:from_qc-check-DET-bY90_to_internal-dpl-injected-dummy-sink"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    - name: from_qc-check-EMC-CellCheckAmplitude_to_internal-dpl-injected-dummy-sink
      type: pull
      transport: shmem
      target: "{{ Parent().Path }}.qc-check-EMC-CellCheckAmplitude:from_qc-check-EMC-CellCheckAmplitude_to_internal-dpl-injected-dummy-sink"
      rateLogging: "{{ fmq_rate_logging }}"
      sndBufSize: 4
      rcvBufSize: 4
    task:
      load: emc-qcmn-flpepn-remote-internal-dpl-injected-dummy-sink
  - name: fairmq-shmmonitor
    enabled: "{{fmq_cleanup_enabled == 'true'}}"
    task:
      load: "fairmq-shmmonitor"
      trigger: DESTROY
      timeout: 10s
      critical: false
